NB:
* via spring_initializr on peut intégrer la spécificité expérimentale "native" au pom.xml
* la fin de l'execution de mvn spring-boot:build-image ne fonctionne qui si docker est trouvé dans le PATH
  et donc plutot à lancer sous linux.